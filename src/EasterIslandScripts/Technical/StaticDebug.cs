using System;
using System.Collections.Generic;
using System.Text;
using UnityEngine;

namespace EasterIsland.src.EasterIslandScripts.Technical
{
    class StaticDebug : MonoBehaviour
    {
        /*
        public void Start()
        {
            Debug.Log("EIStatic START: " + this.gameObject + "->" + GetComponentInChildren<Renderer>().isPartOfStaticBatch);
        }

        void Awake()
        {
            Debug.Log("EIStatic AWAKE: " + this.gameObject + "->" + GetComponentInChildren<Renderer>().isPartOfStaticBatch);
        }

        void OnEnable()
        {
            Debug.Log("EIStatic ENABLE: " + this.gameObject + "->" + GetComponentInChildren<Renderer>().isPartOfStaticBatch);
        }
        */
    }
}
